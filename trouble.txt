** При первом запуске невозможно создать колоду. Приходится обновлять страницу
** После создания карты, изучения ее следующая созданная карта не доступна в опция учить



** RepeatCardsCtrl - при переходе к новой карте, происходит нежелательный переворт текущей карты.
** DataService - _.uniqueId() генерирует уникальный id только в во время выполнения скрипта. Возникают повторные id при работе с сохранёными данными в локальном хранилище

** Проблемы с  getFirstCardToRepeat() в контроллере CtrlDeck, после узучения карточек.


Есть коллекция объектов у которых нужно постоянно проверять одной из свойств (timeToRepeat).

Как я реализую такую проверку:
- Сортируем коллекцию по свойсту timeToRepeat (более рание значения идут первыми)
- Беру первый объект т.е объект который имееют самое ближайшие время повтора
- Помещаю этот объект в атрибут директивы countdown (директива ведет обратный отсчет и извещает о наступление время повтора)

Известить пользователя о том, когда ему нужно будет повторить карточку или сообщить, что время повторения карточкb уже наступило.
*** Можно сделать улучшение 
-------------------------Время------------------------------
---Начало---|---Конец---|---Сл. пов.---
---10:00----|---10:02---|---10:12---
---10:02----|---10:03---|---10:13---
---10:04----|---10:05---|---10:15---


Есть метод в контроллере который инициализурет дополнительные данные в скоуп и перенаправляет на другой роут с этим же самым контроллером. Получаеться, что дополнительные данные пропадут, поскольку контроллер переустановился.

В каком формате лучше хранить интервалы??

	.filter('dhmsFormat1', function($filter) {
	    return function(input) {
	        var arr, formatArr = [];
	        input = input / 1000 -----------------------------------Странный шаг
	        arr = $filter('dhms')(input).split(' ');
	        console.log(input)

	        arr.forEach(function(item) {
	                if(formatArr.length === 2) {
	                    return formatArr
	                }
	                if(parseInt(item) !== 0) {
	                    formatArr.push(item)
	                }
	            });

	        return formatArr.join(' ');
	    }
	    
	});

** при изменении интервала в настройках 